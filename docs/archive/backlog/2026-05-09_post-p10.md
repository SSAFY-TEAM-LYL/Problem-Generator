# 미뤄둔 이슈 — P10 완료 audit (2026-05-09)

> P10 (Output Persistence) 완료 후 audit에서 식별된 이슈 중,
> Option A로 즉시 처리되지 못한 항목들의 보존 기록.
>
> **본 audit가 수행된 commit:** `f197747` (P10 merge — PR #15)
> **fix branch:** `chore/audit-fixes-post-p10`
> **audit 결과:** ruff 0 / mypy 0 / pytest 123 passed (3 skipped) / coverage 87%

---

## P10 audit에서 처리된 항목

| ID | 결과 | 영역 | 처리 |
|---|---|---|---|
| **E1** | ✅ Resolved | `ipe/io.py` | 348 lines > ROADMAP §2 budget ≤280 (+68 초과) → `_render_problem_md` (~82줄)을 `ipe/_io_render.py`로 분리. `save_result`가 `_build_difficulty` 결과를 `difficulty=` 인자로 전달 (순환 import 회피). io.py 267 lines로 감소. (`chore/audit-fixes-post-p10`) |

## P9 audit에서 자연 해소 항목

| ID | 위치 | 해소 commit | 변화 |
|---|---|---|---|
| **D3** | `difficulty_*` 디스크 미저장 | `f197747` (P10) | `problem.json::difficulty` 블록 + `problem.md::## Difficulty` 섹션에 영속화 |

## 미뤄둔 이슈 일람

| ID | 심각도 | 영역 | 핵심 | 검토 시점 |
|---|---|---|---|---|
| **E2** | 🟢 Low | `tests/test_io.py` | `io.py` coverage 99% — line 148 (`_write_generators` 빈 list early return), 162 (`_write_testcases` 빈 list early return) 미커버. 통합 테스트가 happy path만. 단위 테스트 추가 가능 | P12 polish |

## 미해소 이전 audit 항목 (모두 변동 없음)

| ID | 출처 | 영역 | 상태 |
|---|---|---|---|
| **D1** | post-p9 | `sandboxexec_runner.py` coverage (75% — P9 69%에서 환경 변동 회복) | 🔵 P12 CI |
| **D2** | post-p9 | `test_evaluator_unit.py` 미작성 | 🔵 P12 polish |
| **C2** | post-p8 | `tests/test_observability.py` ReplayTracker 단위 테스트 | 🔵 P12 polish |
| **C3** | post-p8 | `main.py` coverage 측정 부재 | 🔵 P12 e2e smoke |
| **B2** | post-p7 | `_executor_helpers.py` 76% (Java compile 분기) | 🔵 P12 CI |
| **B3** | post-p7 | architect 82% / auditor 87% coverage gap | 🔵 P12 polish (E2와 통합 처리) |
| **A4** | post-p6 | `docker_runner.py` 32% (Docker daemon down) | 🔵 P12 CI |
| **A5** | post-p6 | `sandbox/__main__.py` 0% (CLI) | 🔵 P12 e2e |

---

## E2 — `io.py` early return 분기 미커버

### 위치
- `ipe/io.py:148` (`_write_generators` — `if not generators: return`)
- `ipe/io.py:162` (`_write_testcases` — `if not testcases: return []`)

### 문제
P10.4 통합 테스트(`test_save_result.py`)는 happy path full cycle만 검증 →
`generators` / `testcases` 가 항상 비어있지 않은 상태로 호출됨. 빈 list 진입 시
즉시 반환하는 분기 미실행.

### 미뤄둔 이유
- 단순 early return으로 functional risk 낮음
- P12 polish 라운드에서 `test_io_unit.py` 보강 시 `_write_generators(run_dir, [])` /
  `_write_testcases(run_dir, [])` 직접 호출로 검증 가능 (B3 / D2와 통합)

### Resolution 시점
P12 polish 라운드 — B3 / D2 / E2 통합 처리.

---

## 추후 audit 시 체크포인트

다음 audit 라운드 (P11 또는 P12 완료 후) 본 문서를 review:

1. **E2** (io.py early return 분기) — P12 polish에서 단위 테스트 추가?

각 항목 처리 시 해당 행에 ✅ 표시 + 처리 commit hash 기록.
