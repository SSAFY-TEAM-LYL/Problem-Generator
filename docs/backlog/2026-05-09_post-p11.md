# 미뤄둔 이슈 — P11 완료 audit (2026-05-09)

> P11 (Observability — JSON logging + 메트릭) 완료 후 audit.
> Option B (Critical만 즉시 처리, F3/F4는 backlog) 채택.
>
> **본 audit가 수행된 commit:** `ceece5e` (P11 merge — PR #17)
> **fix branch:** `chore/audit-fixes-post-p11`
> **audit 결과:** ruff 0 / mypy 0 / pytest 133 passed (3 skipped) / coverage 87%

---

## P11 audit에서 처리된 항목

| ID | 결과 | 영역 | 처리 |
|---|---|---|---|
| **F1** | ✅ Resolved | `main.py` | 206 lines > ROADMAP §2 budget ≤180 (+26 초과) → `_setup_run(args)` 헬퍼 추출 (run_id/run_dir/traces_dir/db_path 결정 + resume/replay artifact 검증). main.py **161 lines**로 감소 (-45). _parse_args의 help 텍스트 압축 + tracker swap을 ternary로 단순화. (`chore/audit-fixes-post-p11`) |
| **F2** | ✅ Resolved | `ipe/logging_config.py` | 93 lines > ROADMAP §2 budget ≤80 (+13 초과) → docstring/주석 압축, format/setup_logging 본문 단순화. **63 lines**로 감소 (-30). 기능 동일. |

## 미뤄둔 이슈 일람

| ID | 심각도 | 영역 | 핵심 | 검토 시점 |
|---|---|---|---|---|
| **F3** | 🟢 Low | `ipe/observability.py` | coverage 94% — `_load_traces` 의 `ValueError` / `JSONDecodeError` except 경로 (line 194/198-199/202-203) 미커버. P8 audit C2와 동일 (ReplayTracker 단위 테스트 미작성). | P12 polish (C2/E2와 통합) |
| **F4** | 🔵 Info | P11.3 (옵션) | `IPE_LANGSMITH=1` / `IPE_OTEL_ENDPOINT` 환경변수 토글로 LangSmith / OTel trace export. 본 phase에서 보류. ROADMAP §1 P11.3에 (옵션) 표기. | P12 polish (선택적) |

## 미해소 이전 audit 항목 (모두 변동 없음)

| ID | 출처 | 영역 | 상태 |
|---|---|---|---|
| **E2** | post-p10 | `io.py:148/162` early return 분기 | 🔵 P12 polish |
| **D1** | post-p9 | `sandboxexec_runner.py` coverage (75%) | 🔵 P12 CI |
| **D2** | post-p9 | `test_evaluator_unit.py` 미작성 | 🔵 P12 polish |
| **C2** | post-p8 | `tests/test_observability.py` ReplayTracker 단위 테스트 | 🔵 P12 polish (F3와 통합) |
| **C3** | post-p8 | `main.py` coverage 측정 부재 | 🔵 P12 e2e smoke |
| **B2** | post-p7 | `_executor_helpers.py` 76% (Java compile 분기) | 🔵 P12 CI |
| **B3** | post-p7 | architect 82% / auditor 87% coverage gap | 🔵 P12 polish |
| **A4** | post-p6 | `docker_runner.py` 32% (Docker daemon down) | 🔵 P12 CI |
| **A5** | post-p6 | `sandbox/__main__.py` 0% (CLI) | 🔵 P12 e2e |

---

## F3 — `_load_traces` except 경로 미커버

### 위치
`ipe/observability.py:194` (ValueError 경로 — 파일명 prefix가 int로 변환 안 됨),
`ipe/observability.py:198-199, 202-203` (JSONDecodeError + 후속 cache 미할당)

### 문제
P8.3에서 추가한 `ReplayTracker._load_traces`는 traces_dir의 모든 `*.json` 파일을
seq → dict 캐시로 로드. malformed 파일명(non-int prefix) / malformed JSON content
입력 시 except로 fallthrough — 단위 테스트가 happy/empty path만 cover.

### 미뤄둔 이유
P12 polish 라운드에서 ReplayTracker 단위 테스트 (`tests/test_observability.py`)
보강 시 함께 처리 — C2 (ReplayTracker 단위 테스트 미작성)와 동일 카테고리.

### Resolution 시점
P12 polish — C2/F3 통합 처리.

---

## F4 — P11.3 (LangSmith / OTel toggle) 보류

### 위치
미작성. ROADMAP §1 P11.3에 `IPE_LANGSMITH=1` / `IPE_OTEL_ENDPOINT` 환경변수
토글로 trace export 명세.

### 문제
P11에서는 stdout JSON logging + 메트릭 emit만 구현. trace export 통합은 외부
SaaS 의존(LangSmith) 또는 OTel collector endpoint 필요 → dev 환경에서 검증
어려움.

### 미뤄둔 이유
- ROADMAP에서 (옵션) 표기
- DoD 핵심 2개 (JSON 라인 / llm_call_summary) 모두 통과
- LangSmith/OTel 통합은 운영 환경에서 점진적 도입이 자연스러움

### Resolution 시점
P12 polish 또는 운영 라운드 — 별도 commit.

---

## 추후 audit 시 체크포인트

다음 audit 라운드 (P12 또는 polish 완료 후) 본 문서를 review:

1. **F3** (`_load_traces` except 경로) — P12 polish에서 cover?
2. **F4** (P11.3 LangSmith/OTel) — 운영 환경에서 도입?

각 항목 처리 시 해당 행에 ✅ 표시 + 처리 commit hash 기록.
