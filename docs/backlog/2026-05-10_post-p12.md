# 미뤄둔 이슈 — P12 완료 audit (2026-05-10)

> **🎉 12-Phase Roadmap 종료 라운드 audit.**
> P12 (Tests + CLI Polish + CI) 완료 후 최종 audit + 누적 backlog 종합 정리.
>
> **본 audit가 수행된 commit:** `b893bfb` (P12 merge — PR #19)
> **fix branch:** `chore/audit-fixes-post-p12`
> **audit 결과:** ruff 0 / mypy 0 / pytest 133 passed (8 skipped) / coverage 87%

---

## P12 audit에서 처리된 항목

| ID | 결과 | 영역 | 처리 |
|---|---|---|---|
| **G1** | ✅ Resolved | `main.py` | 188 lines > ROADMAP §2 budget ≤180 (+8 초과) → `_apply_exec_workers` 함수(9줄) 제거 + main() 안 walrus inline 3줄 + `_initial_state`의 budget dict 한 줄 컴팩트. main.py **177 lines**로 감소 (-11). 기능 동일. (`chore/audit-fixes-post-p12`) |

## P12 audit 신규 미뤄둔 이슈 (Info만, polish 보류)

| ID | 심각도 | 영역 | 핵심 | 검토 시점 |
|---|---|---|---|---|
| **G2** | 🔵 Info | `tests/e2e/test_smoke.py` | 5 cases는 `ANTHROPIC_API_KEY` 보유 환경에서만 실측 검증. CI(ubuntu/macos)는 secret 미설정 시 skip — manual / nightly trigger 필요. | 운영 환경 nightly cron |
| **G3** | 🔵 Info | `.github/workflows/ci.yml` | push 전 자체 검증 불가 — yaml syntax만 확인. GitHub Actions 결과는 push 후 확인. P12.3 DoD `gh workflow run`도 push 의존. | 첫 push 시 GitHub UI 확인 |

## 🗂️ 누적 미해소 Backlog (12-phase 종합) — Polish 라운드 후보

| ID | 출처 phase | 영역 | 처리 상태 |
|---|---|---|---|
| **A4** | post-p6 | `docker_runner.py` 32% (Docker daemon down) | ✅ Partial Resolved — CI ubuntu runner에서 32% → **59%** (+27%p, run `25608621298`). 잔존 41% 미커버는 isolation_self_test 외 분기 — P12 e2e cycle 추가 시 자연 cover |
| **A5** | post-p6 | `sandbox/__main__.py` 0% (CLI selftest) | ✅ Resolved — P-3 `test_cli_smoke.py` (`506f757`) |
| **B2** | post-p7 | `_executor_helpers.py` 76% (Java javac 분기) | ✅ Resolved — Polish round 3 `tests/integration/test_executor_java.py` 3 cases (javac compile + RTE) → 76% → **88%** (`d94b012`) |
| **B3** | post-p7 | architect 82% / auditor 87% coverage gap | 🔵 Carryover — 노드 단위 테스트 추가 라운드 |
| **C2** | post-p8 | ReplayTracker 단위 테스트 미작성 | ✅ Resolved — P-2 `test_observability.py::TestReplayTracker*` (`17f4556`) |
| **C3** | post-p8 | `main.py` coverage 측정 부재 | ✅ Resolved — P-3 subprocess CLI smoke (`506f757`) |
| **D1** | post-p9 | sandboxexec coverage 변동 (75%) | 🟡 환경 의존 — macOS runner **75% 안정** (`25608621298`), ubuntu는 binary 없어 30% (정상). macOS-only 모듈로 더 이상 변동 없음 |
| **D2** | post-p9 | `test_evaluator_unit.py` 미작성 | ✅ Resolved — P-1 `test_evaluator_unit.py` 11 cases (`a3e750b`) |
| **E2** | post-p10 | `io.py:148/162` early return 분기 | ✅ Resolved — P-1 `test_io.py::TestWrite*` 4 cases (`a3e750b`) |
| **F3** | post-p11 | observability `_load_traces` except 경로 | ✅ Resolved — P-2 `_load_traces` 5 edge cases (`17f4556`) |
| **F4** | post-p11 | P11.3 옵션 LangSmith / OTel 토글 | ✅ Resolved — Polish round 3 `ipe/_tracing.py` (setup_tracing) + `tests/test_tracing.py` 10 cases. `IPE_LANGSMITH=1` → langchain trace 자동 전송, `IPE_OTEL_ENDPOINT` → opentelemetry SDK 활성 (optional dep) (`79228a4`) |
| **A5+** | polish 3 | `sandbox/__main__.py` 0% (subprocess CLI) | ✅ Resolved — Polish round 3 `tests/test_sandbox_main.py` 6 cases (sys.argv monkeypatch + capsys + mock runner). 0% → **96%** (`be7b8c2`) |

**총 11항목** — 모두 Low/Info 등급. 본 phase에서 처리 안 한 이유: ROADMAP DoD 모두 통과 + 누적 항목들이 "polish" 카테고리(coverage gap, dev 환경 한계, optional 기능). 별도 라운드에서 일괄 처리 권장.

### 권장 Polish 통합 그룹

| 그룹 | 항목 | 추정 작업 |
|---|---|---|
| **P-1: 노드 단위 테스트 정비** | B3, D2, E2 | `tests/test_{architect,auditor,evaluator,io}_unit.py` 단위 테스트 보강 (3-4h) |
| **P-2: ReplayTracker 단위 테스트** | C2, F3 | `tests/test_observability.py` 확장 — `_load_traces` malformed JSON / non-int prefix / seq mismatch (1-2h) |
| **P-3: e2e smoke + CLI coverage** | A5, C3 | `subprocess.run(...)` 기반 smoke test로 main.py / sandbox/__main__.py 진입점 검증 (1-2h) |
| **P-4: CI 환경 강화** | A4, B2, D1 | GitHub Actions에 Docker + Java 설치 단계 추가 (1-2h) |
| **P-5: (선택) LangSmith/OTel** | F4 | 환경변수 토글 + 통합 테스트 (3-4h, 운영 환경 의존) |

총 polish 추정: **9-14h** (≈ 1.5일)

---

## 📊 12-Phase 완료 통계

| 항목 | 값 |
|---|---|
| 완료 phase | P0~P12 (13/13) |
| Merged PR | #1~#19 (19 PRs) |
| ruff | 0 issues |
| mypy --strict | 0 errors (27 source files) |
| pytest | **133 passed** + 8 skipped (e2e 5 + Docker 3) |
| **Coverage TOTAL** | **87%** (DoD ≥80% 만족) |
| 신규 모듈 100% coverage | `_io_render.py / calibration / logging_config / runner.py / state.py / __init__.py 4개` |
| TODO/FIXME | 0건 |
| 라인 budget 위반 | 0건 (G1 해소 후) |

## 📊 Coverage 추이 (12-phase 진행)

| Phase | TOTAL | 변화 |
|---|---|---|
| P3 | 72% | initial |
| P5 BETA fix | 78% | +6%p |
| P6 | 79% | +1%p |
| P7 | 84% | +5%p (graph.invoke) |
| P8 | 84% | — |
| P9 | 85% | +1%p (calibration/evaluator) |
| P10 | 87% | +2%p (io.py) |
| P11 | 87% | — |
| **P12** | **87%** | — |

---

## 🎯 ROADMAP All-Green 선언

ROADMAP §1 P0~P12 모든 phase의 Definition of Done 통과:

- ✅ P0 Bootstrap (`83e6bbf`)
- ✅ P1 Sandbox (`3f8d7bb`)
- ✅ P2 LLM Layer (PR #1)
- ✅ P3 Coder + Executor (PR #2 + audit #3)
- ✅ P4 Architect + Phase A (PR #4)
- ✅ P5 Auditor + Phase B (PR #5 + #6)
- ✅ P6 Generator + Phase C (PR #7 + audit #8)
- ✅ P7 Routing & Retry (PR #9 + audit #10)
- ✅ P8 Checkpoint & Replay (PR #11 + audit #12)
- ✅ P9 Evaluator + Calibration (PR #13 + audit #14)
- ✅ P10 Output Persistence (PR #15 + audit #16)
- ✅ P11 Observability (PR #17 + audit #18)
- ✅ **P12 Tests + CLI + CI (PR #19 + audit #20)**

main HEAD: `b893bfb` → audit fix HEAD (post-merge): TBD

---

## 추후 검토 시 체크포인트

Polish 라운드 1차 (`chore/polish-round` branch) 진행 결과:

- [x] **P-1** 노드 단위 테스트 (`a3e750b`):
  - ✅ D2 — `tests/test_evaluator_unit.py` 11 cases
  - ✅ E2 — `tests/test_io.py::TestWrite*` 4 cases
  - 🔵 B3 (architect/auditor coverage) — 별도 라운드로 carryover
- [x] **P-2** ReplayTracker 단위 (`17f4556`):
  - ✅ C2 — `TestReplayTrackerLoadTraces` (5) + `TestReplayTrackerInvoke` (4)
  - ✅ F3 — `_load_traces` ValueError/JSONDecodeError except 경로 cover
- [x] **P-3** CLI smoke (`506f757`):
  - ✅ A5 — `test_cli_smoke.py::TestSandboxCli` (2)
  - ✅ C3 — `TestMainCli` (4) + parametrize (4)
- [x] **P-4** CI 환경 강화 (`000af8f`):
  - 🟡 A4 / B2 / D1 — yaml에 JDK 17 + Docker check 추가; 실측은 push 후
- [ ] **P-5** (선택) LangSmith/OTel (F4) — carryover, 운영 환경 도입 시

### 누적 결과
- ✅ Resolved: 7항목 (A5/C2/C3/D2/E2/F3 + 부분적으로 P-4 yaml)
- 🟡 Partial: 3항목 (A4/B2/D1 — push 후 GitHub Actions 결과로 자연 측정)
- 🔵 Carryover: 2항목 (B3 / F4)

**Roadmap 12-phase 본문은 종료. polish 1차 라운드 완료. 잔여 carryover는 별도 작업.**
